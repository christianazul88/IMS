CREATE DATABASE IF NOT EXISTS testonly123 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- The PHP app creates its three tables automatically on first request.
-- For a non-root deployment, create a MySQL user and set INVITE_DB_USER/PASSWORD.
